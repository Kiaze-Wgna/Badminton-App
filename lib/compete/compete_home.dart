// ranked page indicating ranks and being able to challenge others (real time) in footwork battles
import 'package:flutter/material.dart';

class CompetePage extends StatelessWidget {
  const CompetePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Compete')),
      body: Center(child: Text('Compete Page')),
    );
  }
}
